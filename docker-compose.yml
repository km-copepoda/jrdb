version: '3'
services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    volumes:
    - ../jrdb_DB:/var/lib/pgsql
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin